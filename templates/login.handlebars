<main>
  <div class="container">
    <form action='login.php' method='post'>
      <div class="form-group">
        <div class='container text-center'>
          <p>You drive a <span style='color: red; font-size: 40px;'>{{car}}</span> and your bear's name is <span style='color: green; font-size: 30px; font-style: italic;'>{{bear}}</span>.</p>
        </div>
        <label for 'username'>USERNAME</label>
        <input class="form-control" type='text' name='username' value='' id='username'>
      </div>
      <div class="form-group">
        <label for 'password'>PASSWORD</label>
        <input class="form-control" type='password' name='password' value='' id='password'>
      </div>
      <button class="btn btn-primary" type='submit'>Login</button>
    </form>
  </div>
</main>